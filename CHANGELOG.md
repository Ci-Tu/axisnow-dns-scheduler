# Changelog

## 2.0.0 — 2026-09-25

首个开源版本，前后端全部重写。

### 新增
- Vue 3 + TypeScript + Naive UI 单页应用，统一设计语言，支持浅色 / 深色主题。
- 所有 IP 与线路显示国旗、国家和运营商（DB-IP Lite，自动下载与每月更新）。
- 全局 IP 名称，所有页面共用；1.x 的 `servers.json` 会自动导入。
- 仪表盘新增调度时间线；设置页新增下发记录。
- 凭据可通过环境变量提供（`AXISNOW_API_TOKEN`、`CLOUDFLARE_API_TOKEN`）。
- 调度时区可配置（`APP_TIMEZONE`）。
- 登录失败限流、写接口 CSRF 校验、容器以非 root 用户运行。

### 修复
- 仪表盘前端脚本语法错误导致永远不加载数据。
- 查询拨测状态（POST 形式的读接口）会清空整个读缓存，导致缓存几乎从不命中。
- 拨测采样与仪表盘读错延迟字段，24 小时平均延迟永远为空。
- 删除调度域时本地规则配置的清理逻辑从未生效。
- 列表接口只读取第一页，超过 100 条规则 / 50 个拨测任务时被静默截断。
- 编辑「指定 EIP」规则时地址池会出现重复的 EIP 分组。
- 配置修改中途校验失败时，内存中可能残留半截修改。

### 性能
- 页面接口的上游请求改为并发。
- 读缓存采用 stale-while-revalidate，前端再加一层页面级缓存，切换页面基本瞬时。

### 迁移
- `probe_history.json` 自动导入 SQLite（`data/history.db`）。
